/**
 * 路由跳转，对应着栈的三种操作方式
 */
export const navigationType = {
  pop: "pop",
  push: "push",
  replace: "replace",
};

/**
 * 路由导航方向
 */
export const navigationDirection = {
  back: "back",
  forward: "forward",
  unknown: "",
};
